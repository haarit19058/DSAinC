#include <stdio.h>
#include <stdlib.h>
#include "vector.h"
#include "queue.h"
#include "linkedlist.h"
#include "stack.h"

int main(){
    // vector* v;
    // vec_init(v);
    // for(int i = 1;i<=30;i++){
    //     vec_push_back(v,i*i);
    // }
    // vec_print(v);

    // vec_pop_back(v);

    // vec_print(v);

    // vec_erase(v,8);

    // vec_print(v);

    // vec_insert(v,6,10000);
    // vec_print(v);

    // printf("Size fo the vector is:  %d \n",(int)v->size);
    // printf("At index 10:  %d  \n",vec_at(v,10));


    // queue* q;
    // q_init(q);
    // for(int i = 0;i<=350;i++){
    //     q_push_front(q,i);
    // }

    // for(int i = 0;i<=10;i++){
    //     q_pop_front(q);
    // }
    // for(int i = 0;i<=10;i++){
    //     q_pop_back(q);
    // }
    // // printf("%d\n",q_isfull(q));
    // q_print(q);

    // // printf("\n%d",q->v->data[q->rear]);
    // // printf("%d\n",q->rear);

    // printf("\n%d\n",q_get_back(q));
    // printf("\n%d\n",q_get_front(q));

    // stack* s;
    // stack_init(s);

    // stack_push(s, 10);
    // stack_push(s, 20);
    // stack_push(s, 30);
    // stack_push(s, 40);


    // vec_print(s->v);

    
    // printf("Top element: %d\n", stack_top(s));
    // printf("Stack size: %zu\n", stack_size(s));
    
    // printf("Popped element: %d\n", stack_pop(s));
    // printf("Top element after pop: %d\n", stack_top(s));
    // printf("Stack size after pop: %zu\n", stack_size(s));



    // LL *list;
    // ll_init(list);

    // ll_insertatfront(list,10);
    // ll_insertatfront(list,20);
    // ll_insertatfront(list,30);
    // ll_insertatfront(list,40);

    // ll_insertatback(list,50);

    // ll_print(list);
    // ll_deletebyvalue(list,20);

    // ll_print(list);


    return 0;
}